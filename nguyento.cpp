#include<stdio.h> 
int timax (int a[],int n ){
	int max = a[0] ;
	for ( int i=1;i<n;i++){
		if(a[i]>max) 
		   max =a[i]; 
	} 
	return max;
} 
int main(){
	int n,a[MAX] ;
	scanf("%d",&n);
	printf("nhap mang ") 
} 
